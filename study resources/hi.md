this was a file to test , the problem was i had a directory `study resources`
it was like this :

.
└── study resources
    └── command line
        ├── commands explained in detail.md
        ├── commands.md
        └── The Linux Command Line.pdf

so when it was on github , when i clicked on `study resources` it automatically opened `command line` dirc. So i thought since parent dirc ie. `study resources`
have only one direc. , no other file or direc. , hence it is directly sending to `command line` direc. so i made a file `hi.md` ie. this file . 

- And it turns out to that i was right , after ading the file i was not directed to `command line` dirc.

- The file structure is : (which may change with time)

.
└── study resources
    ├── command line
    │    ├── commands explained in detail.md
    │    ├── commands.md
    │    └── The Linux Command Line.pdf
    └── hi.md
                                